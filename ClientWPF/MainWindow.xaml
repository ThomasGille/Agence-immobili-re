<Window x:Class="ClientWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Gestion de biens immobiliers"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="Titre" TargetType="TextBlock">
            <Setter Property="TextTrimming" Value="None" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,0,5,0" />
            <Setter Property="TextAlignment" Value="Right" />
        </Style>
    </Window.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!-- Affiche les miniatures dans la colonne de gauche -->
        <ListBox x:Name="mListBox" ItemsSource="{Binding liste}" Grid.Column="0" SelectionChanged="ListBox_SelectionChanged">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Titre :" Style="{StaticResource Titre}" />
                            <TextBlock Text="{Binding Titre}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Prix :" Style="{StaticResource Titre}" />
                            <TextBlock Text="{Binding Prix}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="CodePostal :" Style="{StaticResource Titre}" />
                            <TextBlock Text="{Binding CodePostal}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Ville :" Style="{StaticResource Titre}" />
                            <TextBlock Text="{Binding Ville}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Visibility="Collapsed" Text="Id :" Style="{StaticResource Titre}" />
                            <TextBlock  Text="{Binding Id}" />
                        </StackPanel>


                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ListBox>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />
        
        <!-- Affiche les détails dans le block de droite -->
        <ListBox ItemsSource="{Binding Bien}" Grid.Column="2">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Titre :" Style="{StaticResource Titre}" />
                            <TextBlock Text="{Binding Titre}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Prix :" Style="{StaticResource Titre}" />
                            <TextBlock Text="{Binding Prix}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="CodePostal :" Style="{StaticResource Titre}" />
                            <TextBlock Text="{Binding CodePostal}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Ville :" Style="{StaticResource Titre}" />
                            <TextBlock Text="{Binding Ville}" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Visibility="Collapsed" Text="Id :" Style="{StaticResource Titre}" />
                            <TextBlock  Text="{Binding Id}" />
                        </StackPanel>


                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ListBox>

    </Grid>
</Window>
